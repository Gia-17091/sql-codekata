-- 1731. The Number of Employees Which Report to Each Employee
-- LeetCode 기초 (⭐⭐⭐)
-- 문제 링크: https://leetcode.com/problems/the-number-of-employees-which-report-to-each-employee/submissions/1907430390/
-- 작성자: 김하은
-- 작성일: 2026. 02. 04. 12:41

SELECT
    m.employee_id,
    m.name,
    COUNT(e.employee_id) AS reports_count,
    ROUND(AVG(e.age)) AS average_age
FROM Employees e
JOIN Employees m
    ON e.reports_to = m.employee_id
GROUP BY employee_id
ORDER BY employee_id;
